import Columns from 'Columns';
import React from 'react';
import './styles.css';

const App: React.VFC = () => {
    return (
        <div className="App">
            <Columns />
        </div>
    );
};

export default App;
